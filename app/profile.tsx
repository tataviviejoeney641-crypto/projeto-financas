import { Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';
import { useRouter } from 'expo-router';
import { ScreenContainer } from '@/components/screen-container';
import { IconSymbol } from '@/components/ui/icon-symbol';
import { useColors } from '@/hooks/use-colors';

export default function ProfileScreen() { const colors = useColors(); const router = useRouter(); return <ScreenContainer edges={['top', 'left', 'right', 'bottom']} className="px-5"><ScrollView contentContainerStyle={styles.content}><View style={styles.top}><Pressable onPress={() => router.back()} style={[styles.back, { backgroundColor: '#EAF3F4' }]}><IconSymbol name="chevron.right" size={21} color={colors.foreground} style={{ transform: [{ rotate: '180deg' }] }} /></Pressable><Text style={[styles.title, { color: colors.foreground }]}>Meu perfil</Text><View style={{ width: 38 }} /></View><View style={styles.profile}><View style={[styles.avatar, { backgroundColor: '#D9EEEC' }]}><Text style={{ color: colors.primary, fontSize: 22, fontWeight: '800' }}>CM</Text></View><Text style={[styles.name, { color: colors.foreground }]}>Camila Martins</Text><Text style={[styles.role, { color: colors.muted }]}>Analista de Operações · Vendas</Text></View><Text style={[styles.section, { color: colors.foreground }]}>Políticas aplicáveis</Text><View style={[styles.card, { backgroundColor: colors.surface, borderColor: colors.border }]}>{[['Limite por despesa', 'R$ 5.000,00'], ['Prazo para envio', 'Até 7 dias'], ['Recibo obrigatório', 'Acima de R$ 100'], ['Aprovador direto', 'Mariana Costa']].map(([label, value]) => <View key={label} style={styles.row}><Text style={{ color: colors.muted, fontSize: 12 }}>{label}</Text><Text style={{ color: colors.foreground, fontSize: 12, fontWeight: '800' }}>{value}</Text></View>)}</View><Pressable style={[styles.rowAction, { borderColor: colors.border }]}><IconSymbol name="gearshape.fill" size={19} color={colors.primary} /><Text style={{ flex: 1, color: colors.foreground, fontWeight: '700', fontSize: 13 }}>Preferências de notificação</Text><IconSymbol name="chevron.right" size={18} color={colors.muted} /></Pressable><Pressable style={[styles.rowAction, { borderColor: colors.border }]}><IconSymbol name="cloud.fill" size={19} color={colors.primary} /><Text style={{ flex: 1, color: colors.foreground, fontWeight: '700', fontSize: 13 }}>Integração contábil</Text><Text style={{ color: colors.success, fontSize: 10, fontWeight: '800' }}>ATIVA</Text></Pressable></ScrollView></ScreenContainer>; }
const styles = StyleSheet.create({ content: { paddingTop: 14, paddingBottom: 30 }, top: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: 27 }, back: { width: 38, height: 38, borderRadius: 12, alignItems: 'center', justifyContent: 'center' }, title: { fontSize: 19, fontWeight: '800' }, profile: { alignItems: 'center', marginBottom: 27 }, avatar: { width: 74, height: 74, borderRadius: 37, alignItems: 'center', justifyContent: 'center', marginBottom: 12 }, name: { fontSize: 20, fontWeight: '800' }, role: { fontSize: 12, marginTop: 5 }, section: { fontSize: 16, fontWeight: '800', marginBottom: 11 }, card: { borderWidth: 1, borderRadius: 17, paddingHorizontal: 14, marginBottom: 16 }, row: { flexDirection: 'row', justifyContent: 'space-between', paddingVertical: 14, borderBottomWidth: 1, borderBottomColor: '#EDF1EF' }, rowAction: { flexDirection: 'row', alignItems: 'center', gap: 10, borderWidth: 1, borderRadius: 14, padding: 15, marginBottom: 10 } });
